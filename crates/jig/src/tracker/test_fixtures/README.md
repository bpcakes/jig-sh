# Beads compatibility fixtures

These records were generated with `br 0.5.7` by initializing repositories with
the configured prefixes `team:api` and `gh#123`, creating one issue, and running
`br sync --flush-only --force`. The machine-local `source_repo_path` field was
removed before check-in; all producer-owned compatibility fields are otherwise
retained.
