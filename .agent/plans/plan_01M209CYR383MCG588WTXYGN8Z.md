Implement contract epoch and source/resolved declarations, shared bounded string normalization and target-qualified CLI/MCP binding, migrate native migration names with legacy compatibility, verify focused tests and all applicable gates, then run backend test gate.

Implemented v8 bounded string declarations, byte-preserving canonical CLI/MCP binding, native migration-name compatibility for v6/v7, duplicate JSON-key rejection, and declaration inspection. All 20 focused argument tests passed. Saved baseline v7 runtime rejects --contract-version 8 with supports versions 2 through 7. Applicable gates are running using the rebuilt development binary.

Resolved Clippy clone warning, updated current-epoch migration fixtures, and preserved legacy command migration aliases. All 8 migration-policy tests and all 5 UI architecture tests pass. File-budget validation passes after moving render context, MCP tests, migration fixtures, and version probing to their owning modules. Latest core run passed 3049 tests before a dashboard compatibility wording assertion; that wording is fixed and the focused test passes. Full workspace verification is running; refresh remaining required gate evidence before finishing.