Comprehensive repair round 3 of 3. Capture resume-task protected files before submitted imports and verify after every invocation. Make model-command timeouts return structured tool errors after bounded process-group cleanup, preserving outer driver cancellation and descendant ownership. Add real process and import-mutation regressions and update the related CI test path filter. Preserve all index entries and prior state. Run required checks, close this repair work, then obtain the mandatory fresh full-scope review.

Protected resume inputs are now snapshotted before submitted imports and checked after import and every invocation, including alternate-input probes. Command capture now uses owned process groups, non-reaping leader observation, bounded retirement, and output previews; tool timeouts return structured errors to the model, while unconfirmed cleanup aborts the client and is excluded as an execution failure. Driver cancellation permits adapter cleanup before retiring the outer group. Real process tests verify background-descendant retirement, timeout continuation, and outer cancellation. CI paths now include all evaluation test files. Validation: all 38 Python tests passed, focused process/protected-state tests passed, and all five offline smoke families passed with zero model calls. Required profile run_01M28RCJAYVCDQJTM74PNS3EEF passed all six targets, including 4083 Rust tests with 3 configured skips; target validation receipt receipt_01M28RTG7XA54ZV6M4BYHTEB9Q. Tests ran on the current macOS host; Linux-specific supervision awaits native CI coverage. Original index remains unchanged, so repaired files and new modules/tests require staging together. Prior repair evidence is in plan_01M28KH13JN44RM0XAXSPFVCZB and plan_01M28NAT6JDVXWA5DANGRPH0ZK.