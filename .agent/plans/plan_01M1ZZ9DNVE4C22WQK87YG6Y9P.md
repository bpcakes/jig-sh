For jig-sh-g16, split PostgreSQL, SQLite, and no-database integration scenarios with isolated fixtures. Add frontend-free scaffold and cancelled/blocked durable target regression coverage. Document manual migration of project-owned bootstrap commands. Validate focused regressions, configured work gates, and scripts/jig check test through the rebuilt dev runtime.

Implemented the review follow-up. Three independent database cases now commit only the template tree in isolated fixtures. Five focused checks passed in 16.691s, with database cases at 12.513s, 16.319s, and 16.652s. The renderer matrix now covers empty frontend lists for all supported database variants; CLI application presets always populate frontends. Durable cancelled/blocked rows without receipts are asserted even when compatibility results contain only successful started targets. Documented manual migration because project-owned commands and scaffold files remain preserved on update/recopy. Independently ran the generated SQLite setup script twice with real Cargo and only backend .env configuration; the database exists and the example migration and inserted row each occur exactly once. Log: /tmp/jig-review-followup-24hjanca/sqlite-validation.log. Configured gates and the required final check test remain to run.

The initial configured gate attempt could not attest source files that had both staged review content and unstaged follow-up edits. Cancelled that attempt with SIGINT and confirmed terminal exit before staging the five updated source/documentation files. Restarting default work checks against their final staged contents.

Formatting, strict Clippy, core (3132 passed with no slow tests), frontend, and vault partitions passed. The broad contract gate separately found the new Beads export partially staged, so its applicability was unknown. Cancelled the in-progress verification profile before changing the index, staged the final task record, and will rerun default work checks so unchanged scoped partition receipts can be reused and the contract/profile evidence is refreshed.

Final configured work check passed: all six applicable check gates executed successfully, the process gate has correct not-applicable evidence, and the five-target verify profile succeeded with 3899 workspace tests passing and two existing skips. The separately required scripts/jig check test also passed all five targets and 3899 tests (receipt_01M203MCDWC639NMR7WHP3XDNR). Gate and evidence reports show fresh passes; receipt records were inspected. Diff whitespace and append-only JSONL history checks pass. The disposable SQLite fixture was removed; validation logs remain under /tmp/jig-review-followup-24hjanca. The follow-up Bead is closed. No source commit was made.