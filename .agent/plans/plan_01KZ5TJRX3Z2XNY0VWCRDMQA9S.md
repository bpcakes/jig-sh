Implement three behavior-preserving cleanup checkpoints in separate commits: modularize CLI output and move prompt presentation to the CLI layer; remove obsolete Result wrappers from Codex discovery and state diagnostics; decompose status-provider v1 semantic validation into focused helpers. Run the complete workspace test suite before each commit, then finish with repository gates and commit/diff inspection.