Extend the existing picker with static configuration entries and index-based selection so Claude native and explicit modes remain distinct. Reuse layout, search, navigation, terminal cleanup, and signal supervision; preserve Codex inspection behavior. Replace the numbered Claude prompt, update help/docs, add shared-renderer and PTY selection/cancellation tests, run configured gates and finish with the backend test check.