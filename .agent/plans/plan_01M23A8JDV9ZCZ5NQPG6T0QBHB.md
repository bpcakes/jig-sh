Deliver jig-sh-generic-monorepo-zac.4.1 as a versioned design in docs/public-contract.md. Verify exhaustive canonical inputs, transitive source and runner authority, bounded unknown behavior, additive legacy journals, unchanged global execution safety, and original per-target retry provenance. No runtime or schema changes. Validate with the development Jig binary and configured work gates; perform comprehensive review of the final branch.

Drafted the versioned Target Freshness Policy v1 design in docs/public-contract.md. Confirmed that the issue has no child tasks and that runtime implementation remains owned by .4.2/.4.3. First comprehensive review completed with Claude and Codex on identical scope; addressed conflicting empty-input rules, runner authority boundaries, ignored-tree collection, dependency evidence, epoch validation/activation, diagnostic compatibility, and collection cost. Second pass: Codex reported no actionable findings; Claude adapter exited with status 1 and produced no review. Both scope captures matched with no exclusions. Clarified transitive time-boundary inheritance after the second pass. Development binary builds, documentation example parses as TOML, and diff whitespace checks pass. Repository gates and full-branch comprehensive review remain before final completion.

All configured check commands passed, including 3163 core tests, 112 generated-frontend tests, vault/process partitions, and the verification profile with 3930 passing workspace tests and 2 skipped tests. Gate inspection correctly rejected the earlier legacy batch because an unrelated tracker update changed .beads/issues.jsonl while checks ran. The final target profile was fresh, but the export also required its standard machine-local metadata cleanup. Preserve the unrelated tracker change outside this task commit and repeat validation in an isolated checkout. No check result is being promoted from unknown to fresh.

Isolated validation completed on committed design 557747f0. All seven configured check commands and all five verification-profile targets passed; the workspace test result was 3930 passed and 2 skipped. Independent gate inspection reported overall passed, gates_ok true, and all eight gates fresh. Evidence, receipts, work status, JSONL append-only integrity, diff whitespace, and the Beads privacy guard were inspected successfully. The only checkout changes are generated work state. The unrelated tracker update remains outside this feature commit in the original checkout. Final full-branch comprehensive review follows the evidence commit.