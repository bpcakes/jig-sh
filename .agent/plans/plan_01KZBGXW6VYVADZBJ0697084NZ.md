Fix full-update recovery, source-checkout runtime freshness and refresh semantics, safe seed publication, and missing fixture coverage. Run focused reviews, full gates, and repeat comprehensive opus reviews until clean.

Fixed all three verified review findings: full updates now recover missing or malformed contract manifests; source checkouts require explicit JIG_DEV_BIN and honor refresh; seeded binaries are validated before cache publication. Added Rust and shell regressions, enabled the non-Git source fixture in the release entrypoint, stabilized fixture snapshots, and passed targeted plus release-facing fixture validation.

Resolved the first repeat-review findings: repair commands now use capability-only launcher probes; committed pre-v4 templates retain a render-only jig_version compatibility value and have an end-to-end recopy regression; copied development-binary failures and direct MCP development guidance are actionable. During release fixture validation, traced and removed a nested-heredoc deadlock in non-Git source fingerprinting. The complete release fixture suite passes.

The first refreshed workspace check exposed three reproducible owned-process timing failures. Fixed active pipe draining so finite noisy children are not throttled by the idle poll interval; process capture remains memory-bounded and cancellation/deadline checks remain work-bounded. All 27 process-supervision tests and both affected SQLx probe tests pass.

Fixed the fresh full-check failure in jig-vault: brokered output drains now report progress, use a bounded 1024-read burst, and avoid the fixed 10 ms sleep while output is active. The overflow regression now reports its capture-limit error in ~0.5 s instead of timing out after ~22 s; all 99 jig-vault library tests and crate Clippy pass.

Resolved the latest Opus review: malformed .jig.toml now emits a concise parser diagnostic and may reuse only an existing contract-compatible cache (or explicit binary) so doctor/help remain reachable; all help requests use the capability-only probe; manifest-less legacy repair now surfaces the required explicit ownership-adoption follow-up in JSON, human output, and doctor guidance while preserving the no-implicit-ownership boundary. Made launcher parity helper executable. Targeted Rust tests, both touched-crate Clippy checks, shell syntax/parity, and the full release fixture suite pass.

Resolved the second Opus review findings: separated repository-contract probe diagnostics from runtime incompatibility, restored unborn-Git source fingerprinting, added refresh guidance for mutable caches, rejected all non-native PATH wrappers before execution, checked launcher/manifest contract epoch drift in doctor and release validation, reduced active output polling overhead without regressing bounded overflow handling, simplified redundant legacy-file validation, and updated fixtures. Focused process, doctor, vault, shell, parity, full fixture, diff, and Clippy checks pass.

The full workspace gate exposed one stale doctor fixture: it paired the current contract-v4 launcher with TestRepoBuilder's legacy contract-v3 default, so the new epoch-drift check correctly rejected it. Updated the unified doctor fixture to CURRENT_CONTRACT_VERSION; the exact failing readiness test now passes.

A rerun then exposed an unrelated parallel-suite race in the dev-session listener-drop test: the kernel may reassign the released ephemeral port before the raw TCP assertion. The test now verifies that the old authenticated endpoint identity is gone instead of assuming the port number stays unused. The exact regression passed 10 consecutive runs; all 532 jig-dev-proxy tests and its Clippy gate pass.