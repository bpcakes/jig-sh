Compare the archived cookbook with official GPT-6 Astra and GPT-5.6 guidance. Revise the repository guide and source template, regenerate embedded snapshots, and validate template parity and required repository gates.

Research: the cookbook recipe is archived. Current official GPT-6 Astra guidance highlights instruction sensitivity, autonomous follow-through, and proportional verification; GPT-5.6 recommends lean prompts and explicit authorization boundaries. Decision: keep the four living-record sections and observable acceptance, write for a capable repository newcomer, make optional detail conditional, and preserve repository-owned tracking. Updated .agent/PLANS.md and its source template; regenerated the embedded snapshot through the build. Byte parity and git diff --check pass. Required verify gate is running. This change has not been evaluated for model task performance.

Final update (2026-09-10): The initial verification finished successfully with 3,971 tests passed and 2 skipped, plus formatting, Clippy, contract, and file-budget checks. The user then supplied replacement wording and requested that it be used verbatim. That wording now appears in `.agent/PLANS.md`, the source template, and the regenerated embedded snapshot, with identical bytes and line breaks. `JIG_REFRESH_EMBEDDED_TEMPLATE_SNAPSHOT=1 cargo check -p jig-sh` and `git diff --check` passed for the replacement. The full test run predates this wording replacement and is historical evidence, not a fresh gate result for the final text. Implementation is complete; the user requested a pull request. Model behavior has not been benchmarked.
