Root cause: repository epoch was incorrectly used as the lifetime of the command alias NAME interface, and bounded v8 declarations were applied retroactively to native v6/v7 actions. Keep legacy alias semantics independent of repository updates; bind native names using the declared epoch. Validate real update and recopy transactions, old native long-name execution through CLI and MCP, v8 bounds, focused tests, configured work gates, and final backend check.