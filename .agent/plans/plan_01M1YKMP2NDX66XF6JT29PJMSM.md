Implement jig-sh-q1h, jig-sh-fnq, jig-sh-pvd, then jig-sh-i3w sequentially. Confirm each root cause using generic isolated fixtures; cover rendered update/recopy guide coverage, strict SQLx probe output, actual work execution summaries, and credential-free generated dependency bootstrap with explicit database initialization. Preserve authored configuration and receipt compatibility. Run focused regressions per issue, then configured work gates and scripts/jig check test through the rebuilt dev runtime.

Implemented sequentially: q1h preserves explicit Rust guide roots; update/recopy and repository-model regressions pass. fnq accepts SQLx Cargo-subcommand product output with strict versions and trusted executable checks; 35 SQLx doctor tests pass. pvd reports profile targets and run/receipt identities from real execution payloads; enabled previously unregistered evidence regressions; 62 CLI output tests pass. i3w separates dependency bootstrap from generated database setup for Rust and Go; generated Rust PostgreSQL/SQLite/no-database entrypoint and customization-preservation integration passes, plus 36 scaffold tests (one pre-existing registry-dependent skip). Final verification: execute nested-Go script/setup orchestration tests, disposable PostgreSQL role checks, and applicable work gates through rebuilt dev runtime.

Live validation used a generic isolated ExampleProject and a disposable PostgreSQL 18 container. Real Cargo/Bun bootstrap completed without DATABASE_URL or .env. A role with no CREATEDB, CREATEROLE, or SUPERUSER failed to create a missing database with an actionable permission diagnostic; the database stayed absent and privileges stayed false. A pre-created database owned by that role accepted setup twice, with one fixture migration recorded exactly once. Containers were removed. Nested-Go script and setup orchestration regressions also pass. All four beads are implemented and closed; configured final gates remain running.

Real Go/PostgreSQL + React bootstrap also completed with no DATABASE_URL or .env, including sqlc and API client generation. The first broad gate attempt found a cognitive-complexity violation in the new integration test; extracted its update/recopy assertions into a helper without changing behavior. Focused Clippy passes. That older gate attempt was cancelled, and a fresh configured gate pass is now running against the final code and closed bead state.

The broad run passed core and vault partitions but exposed two existing scaffold-runtime tests that still required combined bootstrap/database setup. Updated both tests to preserve ordering assertions across dependency bootstrap and the explicit database script; the complete frontend partition now passes. Because source changed while the earlier batch was running, its evidence is intentionally retained as invalid and all remaining required gate evidence is being refreshed against the unchanged final source.

Final documented scripts/jig check test passed all 3,896 workspace tests (two existing skips), plus Clippy, formatting, contract, and file-budget dependencies. The schema timeout and workflow lease tests each failed once in prior core-partition runs, then passed unchanged both in isolation and in the full suite. All individual gate partitions other than the latest core receipt are fresh and passing. Direct check test with --plan-id was rejected before execution by an unchanged prepared-plan identity mismatch, so the successful full-suite receipt lacks plan attachment. Running the configured work checks once more to refresh the outstanding plan-scoped core and verify evidence; prior failure evidence remains intact.

Final configured work check passed against unchanged source. Core: 3,129 passed; frontend: 112 passed; vault gate passed; process gate has correct not-applicable evidence and was also deliberately run separately with 210 passes. Final verify profile: all five targets succeeded, including 3,896 workspace tests with two existing skips. Gate, evidence, and receipt reports inspected; all required gates passed with fresh applicable receipts. Diff whitespace and append-only state history verified. Disposable manual-validation projects removed; logs retained under /tmp/jig-downstream-manual-validation. All four requested beads are closed; no source commit was made.