Finalize the design after full-branch review: use a read-only gate dependency with generation before planning; name dependency execution-proof storage; specify clean/dirty performance criteria, ignored-dotenv carve-out, and outcome precedence; clarify version and legacy comparisons. Retain the intentional opt-in symlink limitation and whole-repository default. Run the final allowed full-branch review before the final gate validation, so any last corrections are validated together.

Completed the second and final allowed full-branch repeat review at 86c9db44 against base 059255cc. Claude and Codex both completed on fingerprint a362f6a93a40f1b9bf1f35c8af06c1c5e0e1497266d3d97aed748c6965131e3d; parent captures matched, complete and clean, with no exclusions or reviewignore policy. Claude used Opus, restricted file access, and default configuration; Codex used inherited settings. Codex reported no actionable findings. Addressed the final Claude findings by defining 30-second work-check/finish evaluation, an explicit bounded inspection timeout override and fallback procedure, constrained/near-limit benchmark cases, and unambiguous legacy-receipt handling in epoch-E repositories. Also clarified dependency-proof pinning limits and auditing prerequisites for rerun savings. These final documentation clarifications are not subjected to another comprehensive review because the requested two-repeat limit is reached. TOML and whitespace checks pass; final configured gate validation follows the source commit.