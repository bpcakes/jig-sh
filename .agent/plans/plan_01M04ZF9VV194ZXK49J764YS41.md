Generate admin HTTP and API as separate optional deployment units, exclude admin artifacts from public deliverables, make contract generation atomic, add request-ID API errors, and verify generated Rust/React applications.