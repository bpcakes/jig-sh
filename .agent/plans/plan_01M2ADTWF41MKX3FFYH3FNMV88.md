Review repair round 2, new causal group independently identified by Claude and Codex. Map generation writes raw repository paths while the shared checker resolves Markdown URL destinations; a temporary CLI fixture proved generation succeeds but validation rejects literal percent/hash paths and misidentifies percent-encoded-looking filenames. Encode destination bytes, escape Markdown label syntax, and derive links relative to the map location at the owning renderer boundary. Verify generation/check round trips for reserved names and nested maps, retain earlier low-defect regressions, rebuild the dev binary, pass all configured gates, and obtain another fresh full-scope review before the authorized commit. Prior symlink and undefined-reference findings remain rejected as intended behavior.

Round 2 repairs map representation at render(): URL-encode non-unreserved destination bytes while preserving separators, escape Markdown label syntax, and compute repository-root links relative to the map directory. The same generator is used by CLI and bootstrap; ordinary root-map output is preserved. Generation/check round-trip regression covers ten literal directory names at three map depths; all 30 selected guide/map/bootstrap tests passed, including round-1 regression checks. Dev binary rebuilt, both actual repository guide/map policy checks passed, and whitespace/privacy checks passed. No prior repair regressed. Refresh all configured gates before the final independent review.

Run run_01M2ADXY30W1ZCP5SSZVNCRXY5 passed all six required targets: 4102 Rust tests (3 configured skips, no leaky reports), 38 harness tests, Clippy, formatting, contract and file budgets. Validation receipt receipt_01M2AEAKR5W7H9BSKTRJ76A20C. Earlier low-defect regressions remain passing. The map mismatch is verified/fixed; generated percent/hash/space/bracket/Unicode paths and nested-map links round trip correctly. No mitigation remains. Source is settled for the next complete independent Claude/Codex review before committing.