Implement four actionable clusters in separate commits, in this order: DU-001 share the JSONL reader acquisition/retry/explicit-unlock lifecycle while retaining typed and streaming fallback policies; DU-002 canonicalize identical status attempt DTOs with a compatibility name; DU-003 compose detailed gate projection from the borrowed summary projection without retaining nested payload in work rows; DU-004 centralize lexical workspace glob root-escape checks while preserving caller skip/error and traversal policy. Preserve persisted and JSON formats, cancellation, error precedence, MSRV 1.88, and optional dev-proxy builds. Add focused invariant tests for each slice, run focused checks before each commit, build target/debug/jig for harness verification, run configured work check/gates/evidence/receipts and finish backend work with scripts/jig check test. Record progress via work append. Track issue jig-sh-hub. Initial baseline 920367c7. No implementation changes made yet.

DU-001 complete: typed, forward raw, reverse receipt, and test-injected readers now share read_access acquisition/retry/unlock orchestration. Unsupported-lock fallback and parser policies remain in callers; read failures retain precedence, both unlocks are attempted, and replaced files retry before visitors. Added four invariant tests for replacement, no-write missing reads, held/released locks, and error precedence. Focused nextest passed all 263 state/source tests; focused strict Clippy, development build, harness fmt and file-budget passed. The first test compile exposed a missing macro qualification, corrected before validation. Remaining DU-002, DU-003, DU-004, followed by cumulative configured gates and full harness tests.

DU-002 complete: StatusLoopAttempt is canonical for ordinary, waiting, and exhausted status collections. StatusExhaustedAttempt remains a documented compatibility alias; the identity-copy exhausted_attempt_status adapter is deleted. Exact JSON tests cover both exported names, exhaustion values, and present/absent version fields. All 488 selected dashboard, loop, and CLI JSON tests passed; development build and harness fmt/file-budget passed. Recorder wire types and remediation remain separate. Remaining DU-003 and DU-004 plus final cumulative gates.

DU-003 complete: GateView now composes GateSummaryView using its borrowed conversion before moving detail-only fields. Detail rendering reads the shared summary; work rows still retain only compact summaries. Existing display, parity, terminal-safety, and nested-payload exclusion coverage passed: all 62 jig-ui tests. Strict jig-ui Clippy and harness fmt/file-budget passed. No new tests were needed for this small behavior-preserving projection extraction. Remaining DU-004 and final cumulative gates.

DU-004 implementation ready: both workspace discovery callers use jig-typescript::workspace::glob_escapes_root; lexical path semantics, exclusion stripping, caller skip/error behavior, and filesystem traversal remain intact. Updated owning guide. All 112 focused TypeScript/workspace/adoption tests passed, as did CLI no-default-features check and development build. New adoption fixture initially lacked required CI scripts; corrected fixture now verifies positive and exclusion parent components are ignored. All four source slices are implemented; final cumulative work gates and full harness test remain before the last commit and closure.

DU-004 complete and ready to commit: all focused tests and optional-feature checks passed. Cumulative command gates passed (contract, fmt, Clippy, core 3085 tests, frontend 112 tests, process 210 tests; vault gate not applicable). The full verification test target failed in the unchanged scheduled_lease_failure_preserves_worker_receipt_and_retained_worktree fixture after 2800 passing tests. An isolated retry failed at a different stage. Source inspection found a one-second lease and unsynchronized protected-ledger deletion that can race renewal publication. Keep all assertions; stabilize fault injection in a separate follow-up slice, then rerun required verification. Do not close work yet.

Validation follow-up: stabilized the existing lease-failure fixture with a worker-ready handshake, protected-ledger deletion under the actual writer lock, and a 15-second lease. All original cancellation, retained worktree, receipt linkage, and migration error assertions remain. The focused test passed. Run cumulative configured gates against this final source tree before closing.

Lease-failure fixture fix passed in isolation and the core run. Final source checks passed Clippy, fmt, contract, file-budget, and frontend tests. Broad validation exposed four unchanged timing-sensitive tests: a two-second post-lock completion wait, a signal helper readiness wait, and two PR-manager assertions whose one-second backoff had already expired. Core and process gates and the full test target are recorded as failed; no skipped coverage is claimed as passed. The conflict PR-manager case also failed serially. Commit the independent lease synchronization fix, then resolve the reproduced fixture timing issue and rerun failed validation.