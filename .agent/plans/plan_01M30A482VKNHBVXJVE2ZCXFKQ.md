Implement jig-sh-ndz2.1 under docs/plans/agent-workflow-velocity.md T-01 and D-01. Validate gate selection, deduplication, force versus reuse, invalid selection before launches, plan authority, and CLI/MCP behavior. Run focused tests and required gates on a stable candidate, stage all changes, run native Codex review, address findings, then close and commit the task.

T-01 implementation: configured native evidence gates now support explicit forced selection, including optional and overlapping gates; mixed legacy/native gates share execution and unsupported selections fail before child launch. Focused freshness/recovery suite: 43 passed; expanded gate selection suite: 7 passed. First full validation: 4240 tests passed, 3 skipped; Clippy, fmt, contract and source-runtime checks passed, but file-budget rejected growth of checks.rs. Moved combined dispatch into checks/configured.rs; file-budget now passes and checks.rs is 756 lines. A disposable ExampleProject CLI acceptance run invoked the actual emitted native recovery argv: only the failed api target reran (api launch count 2, web launch count 1), ordinary target receipts retained the same plan ID, and gates passed. Final staged checks have passed except the full staged test run, which is still pending. Native Codex review has not yet run.

Final staged validation passed: 4240 tests, three skips, and all six required gates; work check reused all current validations. Gates and evidence passed with the supported 30000 ms read-only inspection budget after the default budget timed out. Native codex review --uncommitted, Codex 0.155.1, reported no actionable findings. Its independent focused test attempt hit a sandbox wait-timeout permission error; full staged validation had already passed outside that review sandbox.