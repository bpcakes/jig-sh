Implement top-level jig codex homes and jig codex launch commands. Discover CODEX_HOME directories, query documented account and rate-limit app-server APIs without reading auth.json, select a home interactively, forward exact Codex arguments, and launch with the selected CODEX_HOME. Keep the commands CLI-only and runtime-owned. Validate parser/help/output behavior, protocol handling, process cleanup, launch planning, documentation, and configured repository gates.

Implemented and dogfooded jig codex homes/launch. Focused Codex suite: 37 passed. fmt, clippy, and contract checks pass. Full jig.test: 1255 passed, 2 ignored, with the same four pre-existing bootstrap::tests::windows_dependency_checker failures observed before implementation; no new failures.

Resolved review findings: interaction stdout is nonblocking, post-cleanup worker retirement is capped at 100ms, app-server protocol messages are capped at 64KiB, and partial nonblocking reads preserve bytes until a newline. Added regressions for an escaped setsid stdout owner, an interaction worker that ignores its deadline, and a newline-free oversized response. Codex-focused suite: 38 passed; escaped-owner tests: 2 passed; fmt, clippy, build, contract, diff check, and six-home live dogfood pass.

Final review-fix verification: 39 Codex-focused tests and 2 interaction-boundary regressions pass; fragmented WouldBlock/UTF-8 reconstruction is covered; live six-home usage inspection remains complete with zero errors.

Resolved all five actionable comprehensive-review findings: made app-server interaction synchronous and deadline-cooperative so no reader worker can outlive timeout cleanup; separated logged-out account state from inspection errors; preserved non-Unix Codex child exit status; corrected the protocol payload boundary to allow exactly 64 KiB plus newline; and improved failed alias resolution diagnostics. Added targeted regressions and updated public/configuration docs. Validation: codex tests 45 passed; interaction regression passed; CLI run tests 10 passed; clippy passed; fmt and contract gates passed; live homes and missing-alias checks passed; full backend suite 1264 passed with only the same four unrelated bootstrap::tests::windows_dependency_checker baseline failures.

Resolved the second comprehensive-review findings: enforce app-server deadlines even under continuous valid protocol traffic; retain observed account data when usage inspection times out, closes, or fails; use stable unknown account status with diagnostic inspection_error; replace lockstep chunks with a rolling four-worker inspection pool; and keep original PathBuf identities through interactive selection with explicit ambiguity handling. Added regressions for continuous notifications, partial usage failure, rolling scheduling, stable status, picker selection/non-UTF-8 paths, and real-launch JSON rejection. Validation: 52 Codex-related tests passed, 3 picker tests passed, escaped-stdout process regression passed, fmt and Clippy passed, live six-home usage report complete with zero errors, contract check passed, and the full backend suite recorded 1271 passed with only the same four unrelated windows_dependency_checker baseline failures.

Resolved the final launcher review findings and clarified CLI behavior: a bare `work` now resolves to `~/.codex-work` before any discovered basename match, while `./work` remains an explicit relative path; inspection panic fallbacks are enriched before progress; interactive inspection no longer holds stderr across worker execution; logged-out homes treat usage as not applicable; weekly/5h labels are scoped to the canonical Codex bucket and verified against returned durations; and dry-run JSON reports lossy non-UTF-8 display. Added isolated discovery, worker-panic, live cancellation, pseudo-terminal progress, and two-home SIGINT regressions. Validation: 65 focused Codex tests and 2 Unix launcher integration tests pass; fmt, Clippy, and contract pass. The full workspace suite recorded 1284 passing and only the same four Windows dependency-checker failures the user explicitly excluded.

Resolved the remaining comprehensive-review findings: malformed account responses are inspection errors instead of logged-out states; an explicit logged-out response completes without waiting for usage; a lone five-hour window is no longer mislabeled weekly; reset times use epoch seconds and omit elapsed resets; protocol EOF distinguishes truncated lines from invalid JSON; home aliases and tilde expansion preserve non-UTF-8 bytes; launcher and agent tooling share the same Codex executable and home configuration; and the pseudo-terminal progress test skips only when the host cannot provide a PTY. Added targeted regressions for each behavior, including byte-distinct lossy path names. Focused validation: 72 Codex-related unit tests and 2 launcher integration tests pass; strict Clippy, formatting, contract, and diff checks pass. The final workspace run recorded 1291 passing and only the same four Windows dependency-checker failures the user explicitly excluded.

Resolved the final merged-review findings: propagate cancellation that arrives after account inspection instead of rewriting it as a usage failure; resolve absolute, explicit relative, and tilde paths without Codex-home discovery; mark lossy non-UTF-8 values in the homes JSON envelope; and suppress inapplicable usage text for logged-out or failed inspections. The production report assembly now has injectable coverage for complete/partial outcomes and discovery/inspection error mapping. Renamed the interaction runner to make its cooperative deadline contract explicit and documented the `codex`/`default` aliases. Focused validation: 78 Codex-related unit tests, 2 launcher integration tests, and the escaped-output-owner interaction regression pass; strict Clippy, formatting, contract, and diff checks pass. The workspace run recorded 1296 passing, the four explicitly excluded Windows dependency-checker failures, and one unrelated load-sensitive SQLx timeout failure that passed immediately when rerun in isolation.


Replaced the numbered Codex-home prompt with a native Ratatui picker on a new shared jig-tui foundation. The picker opens before app-server inspection completes, preserves exact paths, streams indexed updates from the existing four-worker pool, supports fuzzy search, list/detail focus and scrolling, narrow layouts, Enter while loading, and clean cancellation/terminal restoration. Migrated jig-status-tui to the shared lifecycle primitives and documented the controls. Focused evidence: 8 jig-codex-tui tests, 12 jig-status-tui tests, 4 jig-tui tests, 75 Codex-focused jig tests, and 2 Unix launcher PTY/signal tests pass; strict Clippy, fmt, contract, and diff checks pass. The full workspace gate recorded 1294 passing and 2 ignored tests, with only the same four Windows dependency-checker failures explicitly excluded by the user.

Resolved all comprehensive-review findings for the native Codex launcher: supervised external signals through worker cleanup and terminal restoration, fixed final-update ordering and progress repair, bounded detail End navigation, restored worker panic diagnostics, unified the one-window weekly rule, and hardened PTY EOF/restoration tests. Focused tests, strict Clippy, fmt, contract, and diff checks pass. The full workspace gate recorded 1,294 passed and 2 ignored; only the four user-excluded Windows dependency-checker tests failed.

Resolved follow-up comprehensive-review findings: viewport-aware wrapped detail scrolling, stopped-inspection error header, name-first ranked search, Unicode format-control sanitization, explicit PTY test exemption, and production-path account report tests. Focused TUI/Codex tests, all three live PTY tests, strict Clippy, fmt, and contract checks pass. Full test gate: 1294 passed, 2 ignored, with only the four user-excluded Windows dependency-checker failures.

Resolved all final comprehensive-review findings: inspection updates now render before input, discovery warnings remain visible, long-list viewport context persists, clean completion preserves protocol diagnostics, and Unicode joiners remain intact. Corrected ExecPlan timestamps. Validation: 19 picker, 12 status TUI, 5 shared TUI, 76 Codex-focused, and 3 live PTY tests pass; strict Clippy, formatting, contract, guide/map, and diff checks pass. Full workspace: 1295 passed, 2 ignored, with only the four user-excluded Windows dependency-checker failures.

2026-08-04 projection follow-up: the Codex home picker now shows current quota remaining and a linear at-current-pace projection for each returned window, marks the inspected home with the best worst-window projected headroom without reordering rows, and explains the projection in selected-home details. The first 10% of a window remains unranked as collecting; missing timing, unauthenticated homes, and usage failures remain unranked. Added deterministic fixed-time tests for projected remaining, early exhaustion, warm-up suppression, burn-pace ranking, worst-window ranking, and rendered recommendation UX. Updated launch help and configuration/developer UX docs. Validation passed: cargo test -p jig-codex-tui (30 tests), cargo clippy -p jig-codex-tui --all-targets -- -D warnings, cargo test -p jig-sh cli::help_tests::codex_help_distinguishes_homes_from_configuration_profiles, cargo build -p jig-sh --bin jig, scripts/jig check test, scripts/jig check fmt, scripts/jig check contract, and scripts/jig check clippy with JIG_DEV_BIN=target/debug/jig. The existing plan gate summary did not become fresh because its nested contract receipt omitted a worktree fingerprint and retained an older jig.test failure; the direct current-worktree gates above passed.

Validation addendum: added a 50-column compact-layout regression test proving the complete projection stays visible; the final jig-codex-tui suite is 31 passing tests, strict package Clippy remains clean, and the repository test gate passed again.

2026-08-04 comprehensive-review remediation: fixed all merged projection findings. Valid projections now survive unavailable or collecting sibling windows with an explicit partial label while remaining unranked; exhausted windows have a dedicated state, dominate sibling projections, and are never recommended; compact rows name inspection errors, usage errors, signed-out accounts, and unavailable projections textually; recommendation is scoped to visible filtered rows; and projection documentation now follows its example block and accurately distinguishes wide and compact layouts. Kept the requested pace-based ranking metric. Added fixed-time regressions for unavailable/collecting sibling aggregation, exhausted single and multi-window behavior, visible-filter ranking, and compact failure states. Validation: 37 jig-codex-tui tests pass; strict package Clippy passes; jig builds; Codex help contract passes; format, contract, and repository Clippy gates pass. The full workspace run passed all projection tests and 1,338 of 1,339 executed workspace tests, with one unrelated doctor proxy-list timeout under suite contention; that exact failing test passed immediately in isolation.

2026-08-04 merged-review remediation: fixed all three projection follow-ups. Common-width panes now use a responsive Home/Remaining/Projection table with concise complete outcome labels, while compact and truly wide panes retain their richer layouts; fixed-time 100- and 120-column tests prove a non-selected row keeps both remaining quota and its full early-exhaustion projection visible. Timed zero-usage windows now project 100% immediately and remain recommendation-eligible during warm-up. Multiple Codex windows with unknown roles retain their durations in summaries. Updated configuration docs. Validation: 46 jig-codex-tui tests, strict package Clippy, dev jig build, and repository test/fmt/contract/Clippy gates pass. Full test receipt: receipt_01KZ6VPGT5JP3NZ9SJ9ME0AQ4J.

Structured work-check addendum: the plan-linked duplicate workspace run passed all 46 picker tests and 1,339 workspace tests before one unrelated dev_sigint route-publication timeout under suite contention (). That exact integration test passed immediately in isolation. The earlier exact-current-tree repository test gate remains fully green at receipt_01KZ6VPGT5JP3NZ9SJ9ME0AQ4J.

Correction: the isolated passing test was a_different_later_signal_forces_cleanup_without_replacing_the_first.

Resolved the comprehensive review findings: account email remains visible in wide, projection, and compact list layouts; warm-up projections show current remaining quota and risk color while remaining unranked; the full table waits for a 104-cell list pane; picker and human homes output both report remaining quota; search-driven reranking and projection behavior are documented; launcher help wrapping is regression-tested. Fresh validation receipts: test receipt_01KZ707DD6MD6VNPBFFA8PTH44, fmt receipt_01KZ6ZKVH1SMJZAKPF542W56JB, clippy receipt_01KZ6ZMABRQ7YPP0KQY8EYM178, contract receipt_01KZ6ZM1G2XF9RMD6GX0GRWP43.