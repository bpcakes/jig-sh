Implement jig-sh-9wcn.1. Control baseline: 03e9a9e4e5122b5bc12c66b1f635ae1faac05e15. Preserve unmanaged guidance and verification policy; validate representative generated variants, managed-block preservation, template parity, contract, and configured gates.

Resumed task jig-sh-9wcn.1 in the main checkout on feat/astra-harness-modernization (base dc68b744). Kept the existing draft, made full work commands explicit, restored the original fresh-machine setup rule, and made native/legacy evidence and frontend gate wording accurate. Added a Rust/Go v5/v8 rendering check against emitted gate configuration. Unmanaged root guidance is byte-for-byte unchanged. Refreshed embedded snapshots. The first focused compile found a test-only TOML comparison type error, now corrected; verification continues.

Focused verification now passes: native/legacy Rust+Go guidance agrees with rendered gate configuration, Rust-only recopy guidance, embedded snapshot parity, managed-block preservation, and the active four-package-manager generated fixture. The first focused run exposed an unchanged assertion in the active package_managers module; that was updated and its rerun passed. Root unmanaged bytes and snapshot parity are unchanged/matching. Starting configured work gates.

Configured verification found an obsolete renderer assertion requiring a Preferred Commands entry for dev. Replaced it with the preserved mandatory backend test rule and renamed the test to reflect its invariant. All 11 renderer tests now pass. The initial configured run passed Clippy, formatting, contract, and file budget; retrying after the test correction.

Retained the accurate typed-repo-contract introduction after an adoption test exposed that wording dependency. Refreshed snapshots and rebuilt the development binary. All 46 guide-related/renderer/package-manager tests now pass together with fail-fast disabled. Retrying the full required gates against this final source state.

The Go scaffold workflow test required migration-command guidance that had been removed with the catalog. Restored migration/sqlc/schema-documentation commands beside the owning backend rules and exposed application-contract helper commands alongside frontend policy. All 47 focused guide/renderer/package-manager/Go-workflow tests pass after snapshot refresh and rebuild. Retrying required gates.

Acceptance complete for jig-sh-9wcn.1: shortened managed root and shipped guidance; start/check/finish workflow; diagnostics conditional; native reuse wording and legacy gate wording match rendered config; backend verification and migration/compatibility rules retained; unmanaged root bytes unchanged; full/minimal runtime paths unchanged; template snapshot matches. Focused checks: 47 passed. Required gates: all five passed, including 4082 tests with 3 skipped. Successful run run_01M289WM1CWE50CWPBRV7YAAFC; validation receipt receipt_01M28A9NCY7YHW0VJH0M96MG1M. Gate and evidence views both report passed. Implementation is uncommitted on feat/astra-harness-modernization in the main checkout.