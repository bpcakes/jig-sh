Implement the next loop orchestrator phases: read-only github_pr_status workflow with normalized observation schema, attempt exhaustion surfacing and clear-attempt command, then begin worker runner abstraction and PR manager groundwork as feasible. Validate with focused tests and JIG gates.

Progress: completed Phase 2 github_pr_status read-only workflow and attempt-escalation foundation. Added normalized GitHub PR observation schema, gh adapter failure handling with failed tick receipts, pending check exit-code handling, needs_attention attempt surfacing, and loop clear-attempt. Verified with focused loop tests, cargo test -p jig-sh, clippy, fmt, and fresh JIG gates. Remaining: Phase 3 worker runner abstraction and Phase 4 pr_manager mutation workflow/safety rules.

Progress update: Phase 3 worker runner and Phase 4 PR-manager MVP are implemented. Added runtime::worker_runner for Codex exec/review with schema-output support and durable jig.worker_run receipts; work review/refine now link worker receipt ids. Added compiled-in pr_manager workflow kind that reuses the GitHub PR snapshot, skips stacked/cross-repo/draft PRs, uses per-branch leases, prepares isolated git worktrees, runs one Codex worker per tick, commits dirty worktree changes, pushes without force, and records AttemptStore pass/fail around PR worker items. Added tests for worker receipt recording, read-only GitHub PR status, exhausted attempts and clear-attempt, pr_manager worker commit/push, and stacked PR skip idleness. Verification: cargo clippy -p jig-sh --all-targets --locked -- -D warnings passed; cargo test -p jig-sh passed 667 tests; cargo build -p jig-sh --bin jig passed; JIG_DEV_BIN=target/debug/jig scripts/jig work check plus fmt/clippy passed; work gates/evidence are fresh and passing.